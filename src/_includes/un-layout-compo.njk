<div class="composition-card">
   <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
     {% if image %}
       <img src="{{ image }}" alt="{{ title }}">
     {% endif %}
     <div class="content">
       <h3>{{ title }}</h3>
       {{ content | safe }}
     </div>
   </div>
 </div>
